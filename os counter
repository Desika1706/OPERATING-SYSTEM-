#include<iostream>
using namespace std;
int main()
{
	int p1,p2;
	p1=p2;
	cout<<"process "<<"(p1)"<<"is processing";
	cout<<"\n process"<<"(p2)"<<"in the ready queue \n";
	cout<<"\n process"<<"(p2)[child process]"<<"arrival time=";
	
	cin>>p2;
	cout<<"\narrival time of("<<"p2)="<<p2;
	cout<<"\ncurrent processing after p2 enter="<<"p1";
	
}
